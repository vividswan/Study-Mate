{{> layout/header}}
{{^chk}}
<script>
    $(document).ready(function(){
        alert("로그인이 필요합니다.");
    });
</script>
{{/chk}}
<br/>
<div class="container">
    <form action="/api/loginProc" method="post">
        <div class="form-group">
            <label for="username">ID:</label>
            <input type="text" class="form-control" name="username" placeholder="Enter id" id="username">
        </div>
        <div class="form-group">
            <label for="password">Password:</label>
            <input type="password" class="form-control" name="password" placeholder="Enter password" id="password">
        </div>
        <button id="btn-login" class="btn btn-primary">로그인</button>
    </form>
    <p>회원이 아니신가요?  <a href="/join">회원가입</a></p><br>
</div>
{{>layout/footer}}